import { MantineModal } from "@/utils/modals/types";
import ModalLayout from "./ModalLayout";

const SecondModal: MantineModal = () => {
  return <ModalLayout padding={false}>This is second modal</ModalLayout>;
};

export default SecondModal;
